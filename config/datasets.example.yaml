# Example dataset configuration template
# Copy this file to config/datasets.yaml and set real paths.

datasets:
  - name: example_dataset
    source_graph: /path/to/source.rdf
    target_graph: /path/to/target.rdf
    reference_alignment: /path/to/reference_alignment.rdf
    entity_type: class
    language: en
